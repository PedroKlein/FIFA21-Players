import styled from "styled-components";

export const Table = styled.table`
  width: 100%;
  background: var(--light-white);
  border-radius: 10px;

  tr {
    height: 60px;
    background: #36304a;
  }
`;
